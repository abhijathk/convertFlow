{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability granted to the main window — minimal surface: core + open URLs in default browser.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
